import type { MessageStore } from "./buildMessageStore.js";
import type { CsvQuery } from "./buildStore.js";

export function buildDomainExecutionService(invetoryStore: CsvQuery, specificationStore: CsvQuery, messageStore: MessageStore){
  async function name(params:type) {
    
  }
}